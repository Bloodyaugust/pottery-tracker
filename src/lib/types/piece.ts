export type Piece = {
  name: string;
  id: string;
  createdAt: string;
  tags: string;
  photoIDs: string;
  description: string;
  // size: string;
  // weight: string;
  // clayType: string;
  // glaze: string;
  // stages: StageHistory[];
  // firings: Firing[];
};
